[project]
name = "vaani"
version = "0.1.0"
description = "Real-time personal voice translation and meeting assistant"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "soundfile>=0.13",
]

[project.optional-dependencies]
stt = [
    "faster-whisper>=1.1",
    "onnxruntime>=1.20",
]
translate = [
    "transformers>=4.57,<5",
    "sentencepiece>=0.2",
]
voice = [
    "coqui-tts[codec]>=0.27",
    "torch>=2.4",
    "torchaudio>=2.4",
]
cuda = [
    "nvidia-cublas-cu12",
    "nvidia-cudnn-cu12",
]
# Native Windows audio: WASAPI microphones, speakers, and loopback capture.
windows = [
    "soundcard>=0.4.4",
]
all = [
    "vaani[stt,translate,voice,cuda,windows]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vaani"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
